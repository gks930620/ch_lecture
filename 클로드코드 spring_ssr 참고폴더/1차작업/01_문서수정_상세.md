# 1차 작업 — 문서 수정 상세

> 모든 항목은 수정 전에 실제 소스코드와 대조해서 검수 지적이 맞는지 확인함.

## 개념_01_IoC_DI.md

- [높음] "순환 참조 컴파일 시점에 감지" → "앱 시작(컨테이너가 Bean을 생성하는) 시점에 감지" + 서버가 뜨지 않아 바로 발견된다는 보충
- [중간] Step1~4 소제목의 존재하지 않는 파일명(`Step1_DirectCreation.java` 등) → `com.ioc.step1 — Main.java 실행` 형태로 교체 (문서 상단 안내와 일치)
- [중간] `new CommunityRepositoryImpl()` 컴파일 불가 예제 → step1과 같은 `MemberRepository` 가상 예시로 교체 + "Spring Data가 구현체를 런타임 생성하므로 new 불가" 참고 추가
- [중간] `@Component` 예시 `LocalFileStorage`(존재하지 않음) → 실제 존재하는 `LoginCheckInterceptor`, `FileCleanupScheduler`
- [중간] QuerydslConfig 예제 → 실제 코드와 동일하게 `@PersistenceContext` 필드 주입 + 무인자 `jpaQueryFactory()`로 수정
- [중간] Bean 흐름도 — `UserController └── UserService └── UserRepository` 3계층으로 수정
- [낮음] JPAQueryFactory 주입 위치 → `CommunityRepositoryImpl(QueryDSL 커스텀 구현체)` 경유로 수정

## 개념_02_MVC패턴.md

- [중간] CommentApiController 예제 → 실제 시그니처(`/api/communities/{communityId}/comments`, `getComments`, `ResponseEntity<PageResponse<CommentDTO>>`) 기반으로 교체 + "단순화한 버전" 표기
- [중간] 3장 Controller 예제에 "실제 코드를 학습용으로 축약한 버전" 안내 추가 (실제 list/write 시그니처 요약 병기)
- [중간] "뷰 이름 반환 = forward" → "뷰 이름 반환 = 서버에서 바로 렌더링, 진짜 forward는 `forward:` 접두사" 로 정정
- [낮음] 흐름도/표의 "Controller가 실제 비즈니스 로직 수행" → "요청 처리 후 Service에 위임"
- [낮음] `Page<CommunityDTO>` → `PageResponse<CommunityDTO>`
- [낮음] thymeleaf prefix/suffix yml — "자동 설정 기본값(직접 쓰지 않아도 적용됨), 실제 파일엔 cache: false만 있음" 명시

## 개념_00_목차.md / index.md

- [높음] `휘 공공데이터API...` → `공공데이터API, CORS 정리.md` (index.md 링크는 `<...>` 표기로 공백 경로 404 방지)
- [중간] 04·05 결번 → "03에 통합되어 결번" 각주
- [낮음] "프레임워크 라이브러리 차이정도는 해야겠군" 메모 삭제
- [낮음] 개념_10 설명에 `com.aop.step1~4` 경로 명시 (IoC step과 혼동 방지)
- 파일명 변경 반영: `개념_12_스케줄러.md`

## 개념_03_JPA_Entity.md

- [중간] CommunityEntity 예제에 `userId`/`username`/`nickname` 필드 추가 (isWrittenBy 예제와 정합) + isWrittenBy에 주석 보충
- [중간] 커스텀 구현체 네이밍 — "①Custom명+Impl(기본 규칙) ②Repository명+Impl 둘 다 유효, 이 프로젝트는 혼재" 로 재작성
- [중간] "실제 코드 — 게시글 검색"에 "단순화 버전(댓글 수 IN 조회·total null 처리·trim 생략)" 표기
- [낮음] findByUsername 생성 SQL에 `@SQLRestriction`으로 `is_deleted = false` 자동 추가됨 설명
- [낮음] FileRepositoryCustomImpl 예제에 `@RequiredArgsConstructor` + Q클래스 static import 주석 추가
- [낮음] "Java Persistence API" → Jakarta Persistence 병기
- [낮음] yml 발췌에 `defer-datasource-initialization: true` 추가
- [낮음] ddl-auto 경고 — create(데이터 삭제)와 update(스키마 변경 위험) 구분

## 파일테이블설계_정리.md

- [중간] files 테이블 다이어그램에 `file_usage`, `created_at`, `updated_at` 추가, ref_type 예시를 실제 값(COMMUNITY, USER)으로 병기
- [낮음] FileUtil 섹션에 "이 프로젝트에서는 FileService가 이 역할을 겸함" 매핑 문장 추가

## 개념_06_Transaction.md

- [높음] 55~60행 초안 메모 삭제 → REQUIRED 전파 동작 + REQUIRES_NEW 활용을 정식 문장으로 교체
- [중간] `getCommunityDetail` — 조회 섹션에서 쓰기(@Transactional) 쪽으로 이동, "조회수 증가(UPDATE) 때문에 readOnly 해제" 설명 추가
- [중간] REQUIRES_NEW 예시 — "호출하는 쪽에서 try-catch로 잡아야 외부가 영향 안 받음" 조건 추가
- [낮음] 시그니처 — `PageResponse` 반환, createCommunity 4개 파라미터로 수정

## 개념_07_Session_Login.md

- [중간] 4장 두 방법에 "Interceptor 도입 전 방식" 경고 추가 + **4-1장 신설**: LoginCheckInterceptor·WebConfig 실제 코드 요약, mypage에 null 체크가 없는 이유, 댓글 API의 자체 401 JSON 처리
- [낮음] 세션 설정 — "이 프로젝트는 기본값(30분) 사용, 바꾸려면 아래처럼" 으로 수정

## 개념_08_Validation.md

- [낮음→반영] 상단 주의문에 "@Valid가 실행되는 곳이 프로젝트에 없음, 애노테이션 선언만으로는 동작 안 함" 추가
- [중간] CommunityCreateDTO 예제 → 실제 파일 내용(메시지, @Setter, @Builder)으로 교체
- [낮음] `createCommunity(createDTO, null)` → 4개 파라미터 형태 + 주석

## 개념_09_Exception.md

- [높음] 권한 체크 조건 `username != null &&` → `username == null ||` (실제 코드와 동일)
- [중간] GlobalExceptionHandler — 404/403/400/500 **4개 핸들러 전부** 수록 + "구체적 타입 우선 매칭" 설명 추가
- [중간] "@Controller에서 발생하는 예외만 잡음" 단정 완화 (+@RestController가 @Controller를 포함한다는 참고, basePackages 언급)
- [낮음] ApiExceptionHandler 예시 → 실제 코드(`HttpStatus.NOT_FOUND.value()`, `e.getErrorCode()`) + "일부 발췌" 표기
- [낮음] getCommunityDetail 인용에 "발췌·단순화(실제는 @Transactional + 조회수 증가)" 표기, 파라미터명 communityId

## 개념_11_Filter_Interceptor.md

- [높음] 머리말 → "Filter는 코드 없음(개념만) / Interceptor는 실제 적용됨"
- [중간] 5장 세션 캐스팅 `UserEntity` → `LoginUserDTO` (+ Entity 대신 DTO 저장 참고 주석)
- [낮음] WebConfig 인용에 `/uploads/**`, `/css/**`, `/js/**` 제외 경로 추가
- [낮음] 비교표에 "Spring Boot에선 정적 리소스도 DispatcherServlet을 거치므로 Interceptor 적용됨 → exclude 필요" 각주
- [낮음] `javax.servlet` → `jakarta.servlet (구 javax.servlet)`
- [낮음] 3장 예시 — 클래스명 `MyInterceptor`로 변경(5장 실제 코드와 혼동 방지), final 없는 `@RequiredArgsConstructor` 제거, WebConfig 예시에 `@RequiredArgsConstructor` 추가(컴파일 가능하게)

## 개념_12_스케줄러.md (파일명 변경: 스케쥴러 → 스케줄러)

- [낮음] FileService 인용 — `Path uploadPath = ...` 선언 한 줄 포함 + "(일부 생략)" 주석

## 개념_10_AOP.md — 전체 재작성

- 인코딩 손상(U+FFFD 133개 줄)으로 전체 재작성. 기존 목차·코드블록·비교표 구조 유지
- [중간] Pointcut 예시 `execution(* *Service.*(..))` → `*..*Service` 로 통일 + "`*`는 패키지 구분자를 포함하지 않음" 경고 추가
- [중간] `@annotation(Transactional)` → FQN(`org.springframework...Transactional`) + 이유 설명
- [중간] §8 "클래스만 있어도 OK (CGLIB)" — "인터페이스 있으면 JDK 프록시 / 없으면 CGLIB (Boot 웹앱 기본은 CGLIB), step4는 JDK 프록시(`jdk.proxy2.$Proxy##`)가 출력됨" 보충
- [낮음] 용어 표에 **Weaving** 추가
- [낮음] "Spring AOP 주의사항" 섹션 신설 — 자기 호출(개념_06 링크), private/final, non-Bean

## 공공데이터API, CORS 정리.md

- [높음] Simple Request 조건 정반대 → "Preflight가 발생하는 조건"으로 제목 수정 + 올바른 Simple Request 조건 목록 별도 추가
- [중간] RestTemplate "Deprecated" → "유지보수 모드(신규 기능 없음)"
- [낮음] CSRF 주체 명시 — "Spring Security의 CSRF 보호 기능" + 이 프로젝트 미적용 안내
- [낮음] 오타: COSR→CORS, 오리지만→오리진만, `a<img>나`→`` `<img>`나 ``
- [낮음] 사적 메모 2건 삭제 ("나만 이해한거 같아..", "와... 근데 APi 너무 좋다...")
- [추가] CORS 차단 원리 + 프록시 우회 SVG 다이어그램 삽입 (`spring_ssr_images/cors-preflight-proxy.svg`)

## 프로젝트_구조_정리.md

- [중간] supabase 섹션 — "향후 배포 시 개선 계획(현재 미구현, 관련 코드·설정 없음)" 명시
- [낮음] 업로드 경로 → "프로젝트 루트의 uploads/ 폴더"로 일반화
- [낮음] FileController "REST API" 표기 → "@Controller + 파일 응답" 으로 용어 정리 (2곳)
- [낮음] `/api/**` 적용 제외 — "excludePathPatterns가 아니라 addPathPatterns 미등록으로 제외" 각주
- [낮음] 오타 수정: Contorller, innerclasss, compostiion, 서빙에은, @Responebody(전부), "다른 도메인"→"다른 오리진"
- [낮음] 사적 메모("...어려운거 아니니까..") 정리
- 게시글 삭제 흐름에 0번(권한 체크 선행) 반영

## 서비스설계_정리.md

- [중간] "❌ 위험" 패턴 아래에 "이 프로젝트의 delete()가 바로 이 사례" 연결 설명 추가 + validateOwner 선행으로 최소 방어했고 정석은 Facade+@Transactional 임을 안내

## 테스트코드_정리.md

- [중간] MockMvc 예시 — `comments` 검증 라인 삭제(댓글은 JS fetch 구조) + 이유 주석, "실제 존재하는 테스트가 아니라 작성한다면의 예시(현재는 contextLoads 1개뿐)" 명시
- [낮음] "community/list.html" → 뷰 이름은 확장자 없는 `community/list`
- [낮음] 구어체 결론("테스트코드 굳이??") → 정식 결론 문장으로 교체

## SPRING_SSR_정리.md

- [중간] 트리에서 빈 `설계/` 폴더 제거 + "md 문서는 docs/spring_ssr/로 이동됨" 각주
- [중간] 자기모순 해소 — "남아있는 미작성 항목" 낡은 표 삭제 (개념_03·12는 이미 작성 완료 상태이므로 "모든 개념 문서 작성 완료"로 일원화)
- [낮음] `휘 ` 접두사 제거, 스케쥴러→스케줄러 표기 통일
- 게시글 삭제 흐름에 권한체크 선행 반영 (2곳)

## 협업.md

- [낮음] 오타·불명확 문장 정리("PR맞고"→"PR 받고" 등), 메모 수준 문장을 항목별 설명으로 보강 (내용 취지는 유지)
