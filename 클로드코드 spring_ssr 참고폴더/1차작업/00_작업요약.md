# spring_ssr 1차 작업 요약

- **작업일**: 2026-07-05
- **기준**: `1차검수` 폴더의 검수 문서 6개 (심각도 높음 8건 / 중간 21건 / 낮음 31건)
- **원칙**: 검수 지적을 실제 문서·소스와 하나하나 대조해서 **사실로 확인된 것만** 수정. 검수 자체가 틀렸거나 이미 해소된 항목은 수정하지 않고 아래에 기록.
- **검증**: 수정 후 `./gradlew compileJava` **BUILD SUCCESSFUL** (복원한 AOP step1~3 포함 전체 컴파일 통과). AOP step1~3은 별도 javac 컴파일 + `step3.Main` 실행까지 확인.

## 심각도 높음 8건 — 전부 처리 완료

| # | 검수 지적 | 처리 |
|---|----------|------|
| 1 | `개념_10_AOP.md` 한글 인코딩 손상 (133개 줄) | **전체 재작성**. 손상 전 구조가 그대로 남아 있어 원래 목차·코드블록을 복원하고, 검수에서 지적된 내용 오류(Pointcut, CGLIB 등)까지 함께 반영 |
| 2 | `com/aop/step1~3` 15개 파일 0바이트 | **15개 파일 전부 복원**. 문서의 파일 구조 목록과 step4 코드 스타일(OrderService/OrderRepository, 한글 주석)에 맞춰 작성. javac 컴파일 + 실행 확인 |
| 3 | `개념_09_Exception.md:61` 권한 체크 논리 반대 | `username != null &&` → `username == null \|\|` 로 실제 코드와 동일하게 수정 (보안 결함 예제 제거) |
| 4 | CORS 문서 Simple Request 조건이 정반대 | 제목을 "Preflight가 발생하는 조건"으로 바꾸고, 그 아래에 올바른 "Simple Request 조건" 목록을 별도로 추가. "preflght" 오타도 수정 |
| 5 | `개념_01_IoC_DI.md` "순환 참조 컴파일 시점 감지" | "앱 시작(컨테이너가 Bean을 생성하는) 시점에 감지"로 수정 |
| 6 | `개념_06_Transaction.md` 초안 메모 노출 | 메모("전파는 빼자..", `@Trasactional` 오타 등) 삭제 후 REQUIRED 전파 동작 + REQUIRES_NEW 활용을 정식 문장으로 교체 |
| 7 | `개념_11_Filter_Interceptor.md` 머리말 모순 | "Filter는 코드 없음(개념만) / Interceptor는 LoginCheckInterceptor로 실제 적용됨"으로 수정 |
| 8 | `휘 공공데이터API...` 깨진 파일 참조 3곳 | index.md, 개념_00_목차.md, SPRING_SSR_정리.md 모두 실제 파일명으로 수정 (index.md 링크는 공백 경로 안전하게 `<...>` 표기) |

## 검수와 다르게 판단한 항목 — ~~1건~~ → **0건 (정정됨)**

| 검수 지적 | 판단 |
|----------|------|
| 06_총괄 검수 — "application.yml:85 주석도 절대경로 표기" | ~~검수가 현재 상태와 불일치~~ → **작업자 오판이었음 (검수자가 맞음)**. 최초 작업 시 grep이 84행만 잡고 85행의 이어지는 주석(`# 서버 실행 시 ./uploads → C:/workspace/ch_lecture/spring_ssr/uploads/`)을 놓침. 재확인 후 85행 주석을 "`<프로젝트 루트>/uploads/`"로 일반화 **수정 완료** |
| 그 외 지적 | 전수 대조 결과 모두 사실로 확인되어 반영 |

> 결론: 1차검수의 지적은 **전 항목이 사실**로 확인됨.

## 문서 외 소스코드 수정 (5건)

1. `UserController.java` — 주석 `userRepository` → `userService` (실제 final 필드와 일치)
2. `GlobalExceptionHandler.java` — "@Controller 예외**만** 잡음" 주석을 정확한 서술로 완화 (basePackages 언급)
3. `CommunityService.java` — `validateOwner()` 메서드 추가 (권한 체크만 수행)
4. `CommunityController.delete()` — **권한 체크를 맨 앞(0번)으로 이동**. 검수가 지적한 실제 버그(작성자가 아닌 사용자가 삭제 시도 → 파일 고아 처리·댓글 삭제가 먼저 실행된 뒤 403) 수정. 서비스설계_정리.md에 이 사례 연결 설명 추가
5. `data-files.sql` — 주석 처리된 샘플의 original/stored 파일명이 설계 의도와 반대였던 것 수정 (stored는 UUID 기반)

## 파일명 변경

- `개념_12_스케쥴러.md` → `개념_12_스케줄러.md` (`git mv`, 링크 걸린 3개 문서 함께 수정)

## 신규 생성

- `docs/spring_ssr/spring_ssr_images/cors-preflight-proxy.svg` — CORS 차단 원리 + 프록시 우회 다이어그램 (작성자 메모 "그림으로 이해시켜야 될듯" 반영). CORS 문서에 삽입
- `spring_ssr/src/main/java/com/aop/step1~3/` — 15개 파일 복원

## 상세 내역

- [01_문서수정_상세.md](01_문서수정_상세.md) — 문서 18개별 수정 내용
- [02_소스수정_상세.md](02_소스수정_상세.md) — 소스 코드 수정·복원 내용
