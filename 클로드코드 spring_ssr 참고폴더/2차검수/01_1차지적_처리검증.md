# 2차 검수: 1차검수 지적 60건 항목별 처리 검증

> 판정 기준: **해결됨** = 수정이 실제 파일에 반영됐고 수정 내용도 기술적으로 정확 / **부분 해결** = 일부만 이행 / 각 판정은 현재 문서·소스를 직접 대조한 근거 포함.

## 심각도 높음 8건

| # | 1차 지적 | 판정 | 검증 근거 |
|---|---------|------|----------|
| 1 | 개념_10_AOP.md 인코딩 손상 (133개 줄) | **해결됨** | 바이트 검사: U+FFFD 0개, iconv UTF-8 유효, `??` 시퀀스 0건. 전체 재작성 확인(13,189바이트). 잔존 `?`는 정상 문장 2건뿐 |
| 2 | com/aop/step1~3 15개 파일 0바이트 | **부분 해결** | 워킹트리 15개 전부 정상 복원(282~3,864바이트), javac 컴파일 성공 + step1·2·3 Main 3개 실행 재현, gradlew compileJava 성공(검수자 직접 exit 0 확인). **단 git 인덱스는 여전히 빈 blob(e69de29) — 커밋 누락** → [02_신규발견사항.md](02_신규발견사항.md) #2 |
| 3 | 개념_09:61 권한 체크 논리 반대 | **해결됨** | 문서 62행 `username == null \|\|` — 실제 CommunityService.java:82와 완전 동일 + "비로그인이거나 작성자 본인이 아니면 403" 주석 보강 |
| 4 | CORS 문서 Simple Request 조건 정반대 | **해결됨** | 제목을 "Preflight가 발생하는 조건"으로 교체(내용과 정합) + 별도의 올바른 Simple Request 조건 목록 신설 — 새 목록은 CORS-safelisted 스펙과 일치(정확). "preflght" 오타 제거 |
| 5 | 개념_01:238 "순환 참조 컴파일 시점 감지" | **해결됨** | 245행 "앱 시작(컨테이너가 Bean을 생성하는) 시점에 감지 → 서버가 아예 뜨지 않으므로 바로 발견" — 정확 |
| 6 | 개념_06:55-60 초안 메모 노출 | **해결됨** | 메모·오타 완전 삭제. 교체 문장(REQUIRED 전파 동작, REQUIRES_NEW 활용) 기술 검증 통과 — §5 롤백 조건과도 정합 |
| 7 | 개념_11:3 머리말 모순 | **해결됨** | "Filter는 코드 없음(개념만) / Interceptor는 LoginCheckInterceptor로 실제 적용됨 (5장 참고)" |
| 8 | `휘 공공데이터API...` 깨진 참조 3곳 | **해결됨** | index.md:29(`<...>` 꺾쇠 링크 — CommonMark/kramdown 양쪽 유효 문법, 대상 파일 실존), 개념_00_목차:30, SPRING_SSR_정리:221 모두 수정. docs 전체 "휘 " grep 0건 |

## 개념_01_IoC_DI / 개념_02_MVC / 목차 / index (1차검수 01 — 나머지 11건)

| 지적 | 판정 | 근거 |
|------|------|------|
| [중간] 존재하지 않는 파일명 소제목 4곳 | 해결됨 | `(com.ioc.stepN — Main.java 실행)` 형태로 교체, 실제 소스 트리와 일치 |
| [중간] LocalFileStorage @Component 예시 | 해결됨 | LoginCheckInterceptor·FileCleanupScheduler로 교체 — 둘 다 실제 @Component 확인 |
| [중간] QuerydslConfig 예제 불일치 | 해결됨 | @PersistenceContext 필드 주입 + 무인자 메서드 — 실제 파일과 동일 구조 |
| [중간] 컴파일 불가 예제 (new CommunityRepositoryImpl) | 해결됨 | step1의 `new MemberRepository()` 가상 예시로 교체 + "Spring Data JPA 런타임 자동 생성으로 new 불가" 참고문 정확 |
| [중간] Bean 흐름도 Service 계층 생략 | 해결됨 | `UserController └ UserService └ UserRepository` 3계층 |
| [낮음] JPAQueryFactory 주입 위치 | 해결됨 | CommunityRepositoryImpl 경유 관계 명시 |
| [중간] CommentApiController 예제 불일치 | 해결됨 | 실제 매핑·반환타입·메서드명으로 교체 + "단순화 버전" 표기 (잔여: 스니펫에 commentService 필드 선언 누락 — 신규 낮음) |
| [중간] CommunityController list/write 시그니처 | 해결됨 | 실제 시그니처 안내문 추가 — 소스와 정확히 일치 |
| [중간] "뷰 이름 반환 = forward" | 해결됨 | 렌더링/`forward:`/`redirect:` 3가지 구분으로 정정 — 정확 |
| [낮음] Controller "실제 비즈니스 로직" / Page 반환타입 / yml 스니펫 | 해결됨 | "Service에 위임"으로 수정, `PageResponse<CommunityDTO>`로 수정, "자동 설정 기본값" 명시 |
| [높음 외 목차·index] 04/05 결번, 메모, aop step 혼동 | 해결됨 | "04, 05번은 03에 통합되어 결번" 각주, 메모 제거, `com.aop.step1~4` 명시 |

## 개념_03_JPA / 파일테이블설계 (1차검수 02 — 11건)

| 지적 | 판정 | 근거 |
|------|------|------|
| [중간] CommunityEntity 예제 필드 누락 | 해결됨 | userId/username/nickname 추가 — 실제 엔티티와 일치, isWrittenBy 출처 주석 보강 |
| [중간] 네이밍 규칙 상충 | 해결됨 | ①Custom명+Impl(기본) ②Repository명+Impl 둘 다 유효 + 프로젝트 혼재 명시 — 정확 (잔여: 소스 주석은 여전히 뒤바뀜 — 신규 낮음) |
| [중간] "실제 코드 — 게시글 검색" | 해결됨 | "단순화한 버전(생략 목록)" 명시 (잔여: trim 문구 정밀화 — 신규 낮음) |
| [낮음] @SQLRestriction 누락 | 해결됨 | `AND is_deleted = false` 자동 추가 설명 — UserEntity.java:23과 일치 |
| [낮음] ddl-auto update 설명 | 해결됨 | create(데이터 삭제)/update(스키마 변경 위험) 구분 — 정확 |
| [낮음] @RequiredArgsConstructor·static import | 해결됨 | 둘 다 추가 — 실제 파일과 일치 |
| [낮음] Jakarta Persistence 병기 | 해결됨 | 병기됨 |
| [낮음] defer-datasource-initialization | 해결됨 | 추가 + "핵심 설정" 설명 — application.yml:39와 일치 |
| [중간] file_usage 다이어그램 누락 | 해결됨 | file_usage(3종) + created_at/updated_at 추가 — enum과 일치 |
| [낮음] ref_type 예시 / FileUtil 부재 | 해결됨 | "(이 프로젝트: COMMUNITY, USER)" 병기, "FileService가 겸함" 매핑 — FileService.java:70-77과 일치 |
| [낮음] data-files.sql 파일명 반전 | 해결됨 | stored=UUID 형태(`550e8400-....jpeg`), file_path와 일관, 의미 설명 주석 추가 |

## 개념_06 / 07 / 08 (1차검수 03 — 나머지 9건)

| 지적 | 판정 | 근거 |
|------|------|------|
| [중간] getCommunityDetail readOnly 분류 | 해결됨 | 쓰기 섹션으로 이동 + "조회수 증가(UPDATE)로 readOnly 해제" 설명 — 실제 코드와 일치 |
| [중간] REQUIRES_NEW 조건 누락 | 해결됨 | "호출하는 쪽 try-catch" 조건 추가 — 정확 |
| [낮음] 시그니처 불일치(28,31,131행) | 해결됨 | 지적된 행은 모두 수정 (잔여: §2의 update/delete 시그니처는 1차 지적 범위 밖이라 미수정 — 신규 낮음) |
| [중간] LoginCheckInterceptor 부재 | 해결됨 | 4-1장 신설 — 인터셉터 요약·WebConfig 경로·mypage 이유·댓글 API 401 모두 실제 소스와 일치. **단 인용한 소스 설정 자체의 exclude 버그 발견 — 신규 높음 ([02_신규발견사항.md](02_신규발견사항.md) #1)** |
| [낮음] 세션 timeout 문구 | 해결됨 | "기본값(30분) 사용, 바꾸려면 아래처럼" — 정확 |
| [중간] CommunityCreateDTO 인용 | 해결됨 | 애노테이션 5종·메시지 3건 실제 파일과 문자 단위 일치, toEntity() 생략 명시 |
| [낮음] createCommunity(createDTO, null) | 해결됨 | 4개 파라미터 + 세션 출처 주석 (잔여: 변수 미선언 스니펫 — 신규 낮음) |
| [낮음] @Valid 미동작 안내 | 해결됨 | "프로젝트에 @Valid 실행되는 곳 없음" 명시 — 사실 확인 |

## 개념_09 / 11 / 12 (1차검수 04 — 나머지 11건)

| 지적 | 판정 | 근거 |
|------|------|------|
| [중간] 핸들러 2개만+우선순위 없음 | 해결됨 | 4개 핸들러 전부 수록(실제 코드와 일치) + "구체적 타입 우선 매칭" 설명 |
| [중간] "@Controller만 잡음" 단정 | 해결됨 | "나눠 처리하도록 구성" 완화 + @RestController 포함 관계·basePackages 언급 |
| [낮음] ApiExceptionHandler 발췌 미표기 / getCommunityDetail 발췌 | 해결됨 | "(일부 발췌)" 표기 + 실제 코드 사용 방식으로 수정 |
| [중간] 세션 캐스팅 UserEntity | 해결됨 | `LoginUserDTO`로 수정 + "Entity가 아니라 DTO 저장" 주석 |
| [낮음] excludePathPatterns 누락 / 정적 파일 오해 / javax | 해결됨 | 3개 경로 추가, "Boot에선 정적 리소스도 Interceptor 적용 → exclude 필요" 각주, jakarta 병기 |
| [낮음] 3장 예시 혼란 3종 | 해결됨 | MyInterceptor로 개명, 어노테이션 정리 (잔여: 등록 예시에 LoginCheckInterceptor 잔존 — 신규 낮음) |
| [낮음] 파일명 스케쥴러→스케줄러 | 해결됨 | git mv 완료, 옛 파일 없음, "스케쥴러" grep 0건, 참조 3곳 갱신 |
| [낮음] FileService 스니펫 uploadPath | 해결됨 | 선언 포함 + "(일부 생략)" 표기 |

## 총괄 문서 (1차검수 06 — 나머지 15건)

| 지적 | 판정 | 근거 |
|------|------|------|
| [중간] RestTemplate Deprecated | 해결됨 | "유지보수 모드 (신규 기능 없음)" — 코드 주석과 정합 |
| [낮음] CSRF 주체 / 오타 3건 / 사적 메모 2건 (CORS) | 해결됨 | Security 명시+미사용 안내, 오타 grep 0건, 메모 소멸 |
| [중간] supabase 미구현 | 해결됨 | "향후 개선 계획 — 현재 미구현, 관련 코드 없음" 명시 |
| [낮음] 업로드 경로 / REST API 지칭 / api 제외 메커니즘 / 오타·메모 (프로젝트_구조) | 해결됨 | "프로젝트 루트/uploads" 일반화(yml 주석 포함), "@Controller + 파일 응답"으로 정정, "등록 자체를 안 함" 각주, 오타·메모 전부 소멸 |
| [중간] 서비스설계 "위험 패턴 = 실제 delete" 미연결 | 해결됨 | "이 프로젝트 실제 사례: CommunityController.delete()" 인용 블록 신설 — 수정된 코드 상태와 정확히 일치 |
| [중간] MockMvc 실패 예시 | 해결됨 | comments 검증 삭제 + "댓글은 JS fetch 구조" 이유 주석 |
| [낮음] 존재하지 않는 테스트 / list.html / 구어체 (테스트코드) | 해결됨 | "contextLoads() 1개뿐" 명시, "`community/list` (확장자 없음)", 정식 결론 문장 교체 |
| [중간] 빈 설계/ 폴더 / 자기모순 (SPRING_SSR_정리) | 해결됨 | 트리 제거+이동 각주, 낡은 미작성 표 삭제 (잔여: 소제목-본문 어긋남 — 신규 낮음) |
| [낮음] 협업.md 오타·불명확 | 해결됨 | "PR 받고 merge", CSR 문장 명확화+보강 |

## 문서 외 소스 수정 5건 + 신규 SVG

| 항목 | 판정 | 근거 |
|------|------|------|
| UserController 주석 | 해결됨 | `userService`로 수정 — 실제 필드와 일치 |
| GlobalExceptionHandler 주석 완화 | 해결됨 | 문서 개념_09와 동일 취지, 정확 |
| CommunityService.validateOwner() 신설 | 해결됨 | 404→403 순서, null-안전 체크, readOnly 클래스 레벨 적용 적절, 기존 체크와 충돌 없음 |
| CommunityController.delete() 권한 선행 | 해결됨 | validateOwner(195행) → 파일(198행) → 댓글(201행) → 게시글(204행) 순서 확인. `/community/*/delete`는 인터셉터 정상 차단(두 세그먼트) + validateOwner 내 null 체크 2차 방어. 컴파일 성공 |
| data-files.sql | 해결됨 | original/stored 정방향 교정, enum 값 실존, 주석 처리 유지 |
| cors-preflight-proxy.svg (신규) | 문제없음 | 실존, CORS 문서 55행 상대경로 삽입 올바름, 다이어그램 내용(브라우저가 차단·Simple Request 각주·프록시 흐름) 스펙 및 실제 구현과 일치 |
