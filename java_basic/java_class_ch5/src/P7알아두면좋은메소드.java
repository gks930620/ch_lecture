import java.util.Arrays;

public class P7알아두면좋은메소드 {
    public static void main(String[] args) {
        // Arrays.toString()  배열을 주소값이 아닌 원소값들을 반환한다. (즉 출력할 때 쓰면 좋다.)
        //  Arrays.sort(); 배열을 정렬해준다.
        //Arrays.sort(arr1, Collections.reverseOrder()); 내림차순 정렬
       // int tmp[] = Arrays.copyOfRange(arr1, 0, 3);   배열에서 특정 범위 자른 배열

        //Integer.parseInt(문자열)  Long.parse(문자열)



    }

}
