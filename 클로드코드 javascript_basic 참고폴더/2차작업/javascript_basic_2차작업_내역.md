# javascript_basic 2차 작업 내역

작업일: 2026-07-03
작업 기준: `2차검수/` 지적사항 (맞는 지적만 반영, 검수자도 틀릴 수 있음)
전제: 대상 독자 = 개발 입문자

---

## ✅ 완료한 작업

### [높음] 1차 작업이 낳은 회귀 수정 ⭐ 최우선
- **ch7 `part02_선택자와DOM조작.html:516, 528`** — 1차에서 placeholder→인라인 SVG로 교체할 때, SVG 안 `xmlns='...'`의 작은따옴표가 **JS 문자열 바깥 작은따옴표와 충돌**해 `SyntaxError`로 페이지 스크립트 전체가 죽던 문제.
  - 수정: `'src': 'data:...'` → **`'src': "data:..."`** (바깥을 큰따옴표로). node 파싱 OK 검증.
  - **전수 점검**: `'data:image/svg`(작은따옴표로 감싼 SVG) 패턴을 리포 전체에서 재검색 → **다른 곳엔 없음** 확인. (line 414 정적 `<img>`는 HTML 속성이라 정상, 미수정)

### [중간]
- **ch6 "병렬" 용어 통일** — 상단 6.1은 "비동기=병렬 아님(동시성)"인데 하단(강의노트 440~458) 소제목/주석이 "병렬 실행"이라 충돌.
  - 강의노트: "순차 실행 vs **동시 진행**"으로 바꾸고 "CPU 병렬이 아니라 대기시간 겹치기" 한 줄 보강, 함수명·주석 정리.
  - 소스 `part01`(버튼 라벨/로그 3곳): "병렬"→"동시 진행"으로 통일.
- **ch7 `강의노트:444` checkbox `.val()`** — `.val()`은 체크 여부와 무관하게 `value`("on")만 반환. "체크 여부는 `.prop('checked')`/`.is(':checked')`" 주석·예시 추가.

### [낮음]
- **ch6 코드펜스 잔재** — `part01:459` `</html>` 뒤 스트레이 ` ``` ` 제거(1차에서 ch6만 누락됐던 것).
- **ch6 종합예제 정책 일관화** — ch5와 동일하게 **정답코드 종합프로젝트 → "게시판 만들어보기" 요구사항·힌트형**으로 교체, **"강의 진행 팁"(교시별) 섹션 삭제**.
- **ch4 `part03:440`** 체이닝 주석 `(10 + (-5) * 2 = 10)` → `순차 계산: ((0+10)-5)×2 = 10`으로 오해 없게.
- **ch4 `강의노트:293`** Node.js 최상위 `this`는 `global`이 아니라 `module.exports({})`임을 반영("환경에 따라 다름").
- **ch4 비-strict 캐비엇** — `this=window`·`""` 결과가 클래식 스크립트 기준이며 ES모듈/strict에선 TypeError라는 ⚠️ 한 줄 추가.
- **ch3 `part02:318`** falsy 목록의 `-0`이 화면상 `0`으로 보이던 것 → `Object.is(val,-0)`로 `-0` 라벨 별도 표기.
- **ch3 강의노트** 함수 섹션 끝에 "part04 고차함수·IIFE(클로저는 4장)" / 객체 섹션 끝에 "part05 구조분해" **실습 미리보기** 한 줄씩 추가. H1 앞 **선행 공백 제거**.
- **ch1 `part07:21`** "action 비워둔다" 주석 → 실제로 속성이 없으므로 "action 속성을 생략했습니다"로.
- **ch1 `part04`** 상대경로 예시 없던 것 → "상대경로 vs 절대경로" 섹션 추가(같은 폴더 `part03_목록태그.html`로 실제 동작하는 상대경로 링크). 뒤 섹션 번호 재정렬.
- **ch8 `강의노트:176`** "GET Body 사용 불가" → "데이터는 URL(쿼리스트링)로 전달, 관례상 본문 안 씀"으로 정확화.
- **ch9 `part01:74-75`** 소스 비교표 transform/opacity에 `❌*` + "별도 레이어일 때만" 각주 추가(노트엔 이미 있던 것을 소스에도 반영).

---

## 검증
- 편집한 소스 5개(ch7 part02, ch6 part01, ch4 part03, ch3 part02, ch9 part01) **JS 파싱 전부 OK**(node `new Function`).
- 위험 패턴(작은따옴표 감싼 data URI) 리포 전체 0건.

---

## ⚠️ 참고 / 다음 세션(3차)
1. **교시별 "강의 진행 팁" 정책** — 현재 ch5·ch6만 삭제됨. ch3(및 다른 장)에도 교시 섹션이 남아 있음 → 전 챕터 일괄 삭제할지 방침 결정 필요(이번엔 검수 지적 범위인 ch5·ch6만 처리).
2. 나머지 종합예제(ch10 등)도 "안내형" 정책을 적용할지 검토.
3. ch1 `강의노트:93` strong "(SEO 영향)" 표기는 입문 단순화 범위로 판단해 유지(검수도 "유지 가능"으로 표기).
