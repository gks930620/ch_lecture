# java_basic 2차 검수 — 전체 요약

검수일: 2026-07-02~03
검수 범위: 문서 16개 챕터 + index.md + README + 문제답안 4개 + 소스 프로젝트 16개 전체. 1차 검수 결과와 대조하되, 1차에 걸린 것만 보지 않고 **전체를 처음부터 재검수**함. 컴파일(JDK 17)·핵심 버그 런타임 재현·답안 코드 블록 55개 실행 검증 포함.

챕터별 상세는 같은 폴더의 `java_basic_2차검수_chN-chM.md` 참고.

---

## 한눈에 보기

| 챕터 | 1차 지적 반영 | 2차 결과 |
|---|---|---|
| ch1-2 | **25건 전부 정확 반영** | 리그레션 1건(답안의 "우연히 true"), 신규 낮음 다수 |
| ch3-4 | 미반영(전부 잔존) | [높음] switch 타입 주석 오류 그대로 |
| ch5-6 | 미반영(전부 잔존) | [높음] bubbleSort·pow 버그 런타임 재현 확인 |
| ch7-8 | 미반영(전부 잔존) | 신규: 문서의 미정의 `bark()` 예제 |
| ch9-10 | 미반영(전부 잔존) | [높음] hashCode 주석·로또 중복·예외 방침 충돌 그대로 |
| ch11-12 | 미반영(전부 잔존) | [높음] 문서 예제 2건 실행/컴파일 불가 재검증 |
| ch13-14 | **전부 정확 반영, 리그레션 없음** | 신규는 전부 낮음(주석 정돈 수준) |
| ch15-16 | 미반영(전부 잔존) | **[높음] DB 접속 정보·개인정보 노출 그대로 + 노출 위치 추가 발견** |

작업자가 수정한 ch1/ch2/ch13/ch14는 반영 품질이 높음(50건 중 리그레션 1건). **문제는 나머지 12개 챕터가 1차 지적 그대로라는 것** — 1차 요약의 "즉시 수정 필요 15건" 중 13건이 잔존.

## 최우선 수정 (2차 기준)

1. **[보안] ch15 실 DB 접속 정보(`nextit.or.kr` / `std225` / `oracle21c`) 8곳+ 하드코딩** — 그대로. **신규로 개인정보 노출 위치 추가 확인**: 실계정 ID(`gks930620`), 실명, gmail/naver 주소, 전화번호가 `Insert메인.java`, `Update메인.java`, `필요한파일/*.sql`에도 존재. git 이력에 남으므로 비밀번호 변경 검토 필요.
2. ch15 `throw` 누락 4곳, Map key/value 버그, JDBC기본1 리소스 누수 — 그대로.
3. ch5 `bubbleSort`, ch6 `pow` 로직 버그 — 런타임 재현으로 오동작 재확인.
4. ch11 문서 126-129행(실행 시 IndexOutOfBoundsException), ch12 문서 149행(컴파일 불가) — 재검증 완료.
5. ch10 `Object메인.java` hashCode 주석(코드와 정반대), 로또 중복 버그, ch4 `P2switch문2.java:8` switch 타입 오류 주석.
6. ch9 사용자 정의 예외 checked/unchecked 방침 충돌 — **신규: quest/Q3.java도 checked라 세 곳(문서 169행 + InsufficientException + Q3) 동시 수정 필요**.

## 2차 신규 발견 (주요)

- **[중간] 답안 리그레션**: `문제답안/ch2_문제답안.md:190,199` — 문서에서 정정한 "우연히 true" 오류 표현이 신규 답안에 재등장(3자 불일치).
- **[중간] 답안 체계 공백**: `문제답안/`에 ch1/2/13/14만 존재. **나머지 12개 챕터는 답안 문서가 없고** quest 커버리지도 20% 수준. index.md도 문제답안 폴더를 안내하지 않음.
- **[중간] ch6 과제 파일 미커밋 삭제**: `java_class_ch6_객체지향기초/ch6_객체지향기초/` 하위 Part1~Part5·과제_은행시스템 6개 파일이 git에서 미커밋 삭제 상태. 의도 확인 필요.
- [낮음] ch7 문서 129-131행 — instanceof 예제가 정의된 적 없는 `d.bark()` 호출(따라 치면 컴파일 오류).
- [낮음] ch12 문서 71행 — LinkedHashMap "삽입 순서로 LRU 활용" 설명 오류(LRU는 접근 순서 모드 필요, 챌린지 G2와 직결).
- [낮음] ch15 문서 137행 — 미정의 변수 `ds` 사용(DataSource는 9절에서 소개). ch9 문서 111-112행 — "try-catch 이후 코드는 계속 실행" 일반화 부정확.
- [낮음] ch13 함수조합Main — compose 시연이 문서와 다른 짝이라 교차 오해 소지. ch1 IntelliJ 안내가 2025.3 이후 배포 방식과 다를 수 있음.

## 챕터별 파일

- [ch1-ch2](java_basic_2차검수_ch1-ch2.md) / [ch3-ch4](java_basic_2차검수_ch3-ch4.md) / [ch5-ch6](java_basic_2차검수_ch5-ch6.md) / [ch7-ch8](java_basic_2차검수_ch7-ch8.md) / [ch9-ch10](java_basic_2차검수_ch9-ch10.md) / [ch11-ch12](java_basic_2차검수_ch11-ch12.md) / [ch13-ch14](java_basic_2차검수_ch13-ch14.md) / [ch15-ch16+index](java_basic_2차검수_ch15-ch16_index.md)

## 다음 작업 제안 (작업자용)

1. ch15 보안 조치(접속 정보·개인정보 제거, 비밀번호 변경 검토) — 다른 무엇보다 먼저.
2. 1차 "즉시 수정 필요" 잔존 13건 처리(ch3~12, 15~16).
3. ch2 답안의 "우연히 true" 정정, ch6 삭제 파일 커밋 여부 결정.
4. 나머지 12개 챕터 문제답안 작성 + index.md에 문제답안 안내 추가.
