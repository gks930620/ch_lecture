# java_basic 2차 검수 — ch1 시작하기 / ch2 변수와자료형

검수일: 2026-07-02~03 (2차)
검증 방법: 1차 검수서·작업 보고서 대조 → 문서 2건·답안 2건 전문 정독 → 소스 20개 파일 전수 확인 → JDK 17로 소스 전체 및 **답안 코드 블록 18개 추출·컴파일·실행하여 예상 출력과 대조** → 이미지 10개 실존 확인.

**1차 반영 상태: 1차 지적 25건 모두 정확히 반영됨** (BOM 제거, Main.java 삭제, HelloWorld.java/P9 신설, Scanner 절 신설, "우연히 true" 문서 정정, 오타·주석·throws 정리, quest 매핑, 확인 문제 신설). 소스 전체 컴파일 통과, 답안 코드 18개 전부 실행 결과가 예상 출력과 일치. **리그레션은 답안 문서의 "우연히 true" 1건.**

---

## 중간

1. [신규] **답안이 문서 정정 내용과 모순 — "우연히 true" 표현 재등장** — `docs/java_basic/문제답안/ch2_문제답안.md:190, 199`
   1차 검수로 문서(`ch2_변수와자료형.md:229`)는 "풀 공유로 항상 true"로 정정됐는데, 신규 답안 B-5에 같은 오류가 재등장: 190행 `// true (우연히 true — 여기에 의존하면 안 됨!)`, 199행 "리터럴끼리는 우연히 true가 나오기도 하고". 리터럴 간 `==`는 JLS가 보장하는 규칙(항상 true)이므로 기술적으로 틀렸고, 학생이 문서와 답안에서 상반된 설명을 보게 되는 3자 불일치. 583행(F-2) "우연히 리터럴 풀 덕에 동작하더라도"도 같은 맥락으로 수정 필요.

## 낮음

2. [신규] **문서-답안 선수 챕터 표기 불일치** — `ch2_변수와자료형.md:466~491` vs `문제답안/ch2_문제답안.md:269, 299, 587` — 답안에는 C-4/D-1/F-3에 "ch4 학습 후" 표기가 있으나 문서 문제 목록에는 없음(1차 때 B3/B4/D2/F2만 추가됨). 문서만 보고 푸는 학생은 C-4에서 안 배운 if문을 만남.
3. [신규] `docs/java_basic/ch1_시작하기.md:25` — "Community Edition 다운로드" 안내가 JetBrains 2025.3 이후 단일 배포판(무료 티어 포함) 방식과 맞지 않을 수 있음. 완충 문구 검토 권장. 30행 `Project SDK` 필드명도 최신 마법사에서는 `JDK`로 표기.
4. [신규] 실행해도 출력이 없는 실습 파일 — `src/ch2_variable/P2정수.java`(println 없음), `P8Scanner.java:8`(입력 안내 없이 nextInt() 호출 → 멈춘 것처럼 보임, 문서 §8 예시와 결이 다름).
5. [신규] 미사용 지역 변수 — `P5문자열.java:5~6`(str1, hello), `P1변수.java:5`(score).
6. [신규] `ch1_시작하기.md:23` — `### 1.0   IntelliJ IDEA 설치` 이중 공백, "1.0" 절 번호 재검토 여지.
7. [신규] `ch2_변수와자료형.md:398` — §8 안내의 실습 파일 목록에 `P7변수사용범위와입력.java` 누락(Scanner 사용 파일).
8. [1차 잔존] `ch2_변수와자료형.md:334~345` — §6.4 instanceof 패턴 매칭 블록이 그대로 유지됨(1차 "ch7 이후로 미루는 것 고려" 제안). 작업 보고서의 미반영 목록에도 기재 안 됨 — 의도적 유지라면 보고서에 한 줄 기록 권장.

## 이미지 보완 제안

9. [신규] `ch2_변수와자료형.md:347~355` §6.5 오토박싱/언박싱 — "null인 Integer를 int로 푸는 순간 NPE" 흐름 그림 추가 권장(답안 C-4와도 연결).
10. [신규] `ch2_변수와자료형.md:202~213` §4.1 String 불변 — `s.toUpperCase()`가 새 객체를 만드는 과정 그림 추가 권장.

## 이상 없음 확인
- ch1 소스 3개, ch2 소스 14개 전부 javac 17 통과. 답안 코드 18개 컴파일·실행 성공, 예상 출력 전부 일치(-1294967296, -126, 2.0, 합계 210, 표준편차 13.04 등 수치 검산 완료).
- 문제-답안 대응: ch1 5문항/ch2 25문항 모두 답안 존재·대응 정확.
- 이미지 10개 실존, BOM 제거 확인(답안 포함 4개 md), Main.java 삭제·HelloWorld.java·P9핵심예제모음.java 신설 확인.
