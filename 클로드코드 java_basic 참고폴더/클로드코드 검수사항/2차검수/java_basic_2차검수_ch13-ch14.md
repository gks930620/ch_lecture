# java_basic 2차 검수 — ch13 람다와함수형 / ch14 stream

검수일: 2026-07-03 (2차)
검증 방법: **답안 문서 37개 코드 블록 전부 추출해 JDK 17로 개별 컴파일·실행, 출력 주석과 대조.** 실습 소스 전체 컴파일 + 주요 메인 실행. 신규 SVG 2개 실존·내용 확인.

**1차 반영 상태: ch13/ch14 해당분 전부 정확히 반영, 리그레션 없음.**
- Human.java(`Human{`/`instanceof Human`), 커스텀집계(map 분리 후 reduce, 실행 결과 80), 스트림4요소수집(같은 패키지 Person + groupingBy 출력), Solution.java·jdk.jfr import 삭제, "10살 초과" 문구 통일, 오타 4건, @FunctionalInterface 5개 부착, 신규 P4/P5/Q5/P4옵셔널과병렬 정상 동작 확인.
- 답안 코드 37블록 전원 컴파일 통과, 실제 출력이 출력 주석과 전부 일치.
- 신규 SVG 2개(function-composition, collectors-groupingby) 실존하며 내용이 문서 설명과 일치.

---

## [신규] 발견 (전부 낮음 — 높음/중간 없음)

- [신규][낮음] `java_basic/java_class_ch14_stream/src/P1스트림이란/스트림메인.java:9-10` — 강의 준비용 개발 메모 노출("어쨋든 예제는 2개 합쳐서 만들어야지", "내부반복자라는거 중요"). 오타 "어쨋든"→"어쨌든".
- [신규][낮음] `src/P3스트림최종처리/스트림3요소커스텀집계.java:18` — 주석 "10의자리만 남겨야징" 구어체·띄어쓰기. 1차 수정 시 옆 줄만 고치고 남긴 것.
- [신규][낮음] `java_basic/java_class_ch13_람다와함수형/src/P4표준함수형인터페이스/표준함수형Main.java:25` — 신규 파일의 조사 오류: "민지을 출력합니다."(→ "민지를").
- [신규][낮음] `src/P5함수조합/함수조합Main.java:17-20` vs `docs/java_basic/ch13_람다와함수형.md:144-145` — 문서는 `g.compose(f)`(= `f.andThen(g)`, 항상 같은 결과)를 설명하는데 소스는 `trim.compose(소문자)`(실행 순서가 반대인 다른 함수, 이 입력에서만 우연히 결과 동일)를 시연. 교차해서 읽으면 "compose는 andThen과 항상 같다"로 오해 소지 — 소스 예를 문서와 같은 짝으로 맞추거나 주석 한 줄 보강 권장.
- [신규][낮음] `src/P3스트림최종처리/스트림3요소기본집계.java:18,23` — ① 주석 "애네는"→"얘네는", 여는 괄호 미닫힘 ② `max(...).get()` 사용 — 문서 §7 "get() 남용 대신 orElse/orElseThrow 권장" 가르침과 어긋나는 시범.
- [신규][낮음] `java_basic/java_class_ch13_람다와함수형/src/P3메소드참조/메소드참조main.java:14-16` — 정렬만 하고 println이 없어 실행해도 아무것도 안 보임.
- [신규][낮음] ch13/ch14 `src/Main.java:1` — IntelliJ "Hello world!" 템플릿 잔존. ch1/ch2에서는 삭제했으므로 일관성 차원 삭제 후보.
- [신규][낮음] `src/P2스트림중간처리/스트림1스트림얻기메인.java:16` — 주석 처리된 죽은 코드.

## [1차 잔존] (의도적 보류 포함)

- [1차 잔존][낮음] `스트림메인.java:3-5` 미사용 import — 작업 문서 §6에서 "다음 차수 일괄 정리"로 보류 명시된 항목.
- [1차 잔존][낮음] ch13 문서 §8 예외 처리 대응 실습 소스 없음(답안 F-1 코드로만 커버).
- [1차 잔존][낮음] ch14 quest에 E-3(병렬 측정)·F 챌린지 대응 코드 없음 — 답안 문서 신설로 실질 완화됨.

## 결론
1차 수정은 전부 사실대로 반영됐고 리그레션 없음. 신규 답안 2개 파일은 전 블록 컴파일·실행·출력 검증 통과로 품질 높음. 남은 것은 전부 "낮음" 등급의 주석 정돈과 compose 예제 미세 보완 정도.
