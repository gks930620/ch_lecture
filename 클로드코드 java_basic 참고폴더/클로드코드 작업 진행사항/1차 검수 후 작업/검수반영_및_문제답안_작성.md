# 1차 검수 후 작업 — 검수사항 반영 + 문제 답안 작성 (2026-07-02)

## 작업 범위

1. `클로드코드 검수사항/1차검수` 폴더의 검수 문서 2건(ch1-ch2, ch13-ch14) 반영
   - 원칙: **검수 항목을 하나하나 실제 파일과 대조해 사실인 것만 수정** (검수자 오류 가능성 검증)
2. 검수와 별개로 ch1/ch2/ch13/ch14 **문제 예시 답안** 작성

## 결과 요약

- 검수 지적 **총 25건 검증 → 25건 모두 사실로 확인되어 전부 반영** (검수 오류 0건)
- 문서 4개 + 실습 소스 20여 파일 수정, 실습 소스 신규 5개, 답안 문서 4개, SVG 2개 신규
- ch1/ch2/ch13/ch14 실습 소스 전체 **JDK 17 컴파일 통과 확인**, 신규/수정 메인은 실행 결과까지 확인

---

## 1. 검수 반영 — 문서 (docs/java_basic)

### ch1_시작하기.md
- **BOM(U+FEFF) 제거** — 제목 줄 앞에 숨어 있어 렌더러에 따라 깨진 문자로 보일 수 있었음 (검수 지적 사실)
- 오타 수정: "컴파일 ," → "컴파일,", "클래스 로딩,  GC" 이중 공백 제거
- 비문 수정: "인텔리제이 java 설치는..." → "IntelliJ로 설치한 JDK는 환경변수(PATH) 등록 없이 IntelliJ 내부 설정을 통해 실행된다"
- 4.6 메모리/GC 절에 "객체/참조는 ch6에서 배움, 지금은 큰 그림만" 안내 추가
- **"확인 문제" 섹션 신설** (5문항: JDK/JRE/JVM 구분, 컴파일 산출물, WORA, JDK 필요 이유, 컴파일 vs 실행 오류) + 답안 링크

### ch2_변수와자료형.md
- **"리터럴끼리 ==가 우연히 true" 표현 정정** — 검수 지적이 맞음: 풀 공유로 **항상** true. "오히려 착각하기 쉽다"로 수정
- 배열 for 예제에 "반복문은 ch4에서 배움" 안내 추가
- `Arrays.copyOf` 예제에 `import java.util.Arrays;` 필요 주석 추가
- **"8. 입력 받기 맛보기 — Scanner" 절 신설** (실습 소스 P8Scanner/quest가 Scanner를 쓰는데 문서에 설명이 없던 단절 해소, 기존 8번 절은 9번으로)
- 문제 선수 챕터 표기: B3/B4(ch4), D2(ch5), F2(ch5) 추가
- 문제 섹션에 quest Q1~Q4 매핑 안내 + 답안 링크 추가

### ch13_람다와함수형.md
- `isBlank()`(Java 11+), `UUID` 한 줄 설명 주석 추가
- §6 함수 조합: 실행 예(`normalize.apply(" Hello ") // "hello"`)와 andThen/compose 차이 설명 추가
- 신규 실습 파일 안내(P4/P5), 문제 섹션에 답안 링크 추가

### ch14_stream.md
- `Function.identity()`/`Collectors.counting()` 역할 주석 추가
- 신규 실습 파일 안내(P4옵셔널과병렬), 문제 섹션에 답안 링크 추가

## 2. 검수 반영 — 실습 소스 (java_basic)

### java_class_ch1_시작하기
- `src/Main.java` **삭제** — IntelliJ 자동 생성 템플릿 쓰레기 (printf 개행 없음 확인)
- `src/HelloWorld.java` **신규** — 문서의 `javac HelloWorld.java` 실습과 일치하는 파일이 없었음

### java_class_ch2_변수와자료형
- `src/Main.java` **삭제** — 챕터와 무관한 코딩테스트 풀이(페인트 구역 계산) 확인
- `P6연산식자동타입변환.java` — 잘못된 대안 주석 `// or (byte)x+byte(y)` 삭제 (문법 오류 + int 승격 문제)
- `P2정수.java` — 깨진 주석 → "소문자 l은 숫자 1과 헷갈리니 대문자 L을 사용하세요"
- `P4문자.java` — "asc22"→"ASCII", 미구현 TODO(알파벳 출력)를 for문으로 구현(+ch4 안내)
- `P3논리.java` — "뉴진스에→뉴진스의", boolean 크기 주석을 문서 표와 통일("JVM 구현에 따라 다름")
- `quest/Q2.java` — `permiter`→`perimeter` (3곳), `quest/Q4.java` — "갯수"→"개수" (4곳)
- `P7변수사용범위와입력.java` — 불필요한 `throws IOException` 제거, 읽은 값 미사용 → 출력 추가
- `과제_계산기.java` — 빈 스텁에 과제 요구사항(입력/출력 예시) 주석 추가
- `ch2_variable/P9핵심예제모음.java` **신규** — 문서 핵심 예제(오버플로우/부동소수점 오차/String 풀/배열 복사/final/var)에 대응하는 실습 파일이 없던 것 해소

### java_class_ch13_람다와함수형
- 인터페이스 5개(Workable/Maxable/Printable/Singable/Sumable)에 `@FunctionalInterface` 부착
- `람다기본메인3.java` — 괄호 유지/생략 버전을 나란히 비교하도록 수정
- 미사용 import 제거: `함수형람다Main2.java`(4개), `메소드참조main.java`(2개)
- **신규**: `P4표준함수형인터페이스/표준함수형Main.java`, `P5함수조합/함수조합Main.java`, `quest/Q5.java`(함수 조합 파이프라인) — 문서 §3/§6과 실습의 커버리지 격차 해소

### java_class_ch14_stream
- **`P3스트림최종처리/Human.java` 복붙 버그 수정 (검수 "높음" 등급)** — toString이 `"Person{"` 반환, equals가 `instanceof Person` 검사 → Human으로 수정. 이 버그로 distinct 중복 제거가 절대 동작하지 않았음
- `스트림3요소커스텀집계.java` — reduce 계약 위반(누적값에도 절삭 적용) → `map(age/10*10).reduce(0, Integer::sum)`으로 수정
- `스트림4요소수집.java` — 다른 패키지의 동명 Person import → 같은 패키지 Person 사용, groupingBy 결과 출력 추가
- `src/Solution.java` **삭제** — 베스트앨범.java와 중복 + `com.sun.jdi` 미사용 import + main 없음 (3가지 모두 확인)
- `스트림메인.java` — 무관한 `jdk.jfr.Period` import 제거
- `스트림2요소조건만족메인.java` — 조건(`> 10`)과 문구("10살 이상") 불일치 → "10살 초과"로 통일
- 주석 오타 4건(distcinct/avreage/단수히/도우ㅏ줌), findFirst 분류를 "집계"→"탐색"으로 수정
- **신규**: `P4옵셔널과병렬/옵셔널병렬Main.java` — 문서 §7 Optional/§8 병렬에 대응하는 실습

## 3. 문제 답안 작성 (신규 — docs/java_basic/문제답안/)

| 파일 | 내용 |
|---|---|
| `ch1_문제답안.md` | 신설한 확인 문제 5문항 답안 (개념 설명형) |
| `ch2_문제답안.md` | A~F 25문항 전체 — 실행 가능한 코드 + 예상 출력 주석 + 핵심 포인트 해설. 선수 챕터 필요한 문제는 표기 |
| `ch13_문제답안.md` | A~F 18문항 전체 — 함수형 인터페이스 선택 이유 해설, F-1은 ThrowingFunction 래퍼 완성 코드 |
| `ch14_문제답안.md` | A~F 18문항 전체 — 그룹핑은 TreeMap으로 출력 순서 고정, 병렬은 시간 측정 + "환경에 따라 다름" 명시 |

각 챕터 문서의 문제 섹션에서 답안 파일로 링크 연결. 답안에 이미지가 필요한 문항은 없다고 판단(코드+출력으로 충분).

## 4. 신규 이미지 2개 (검수의 다이어그램 제안 반영)

- `ch13/function-composition.svg` — " Hello " → trim → toLowerCase → "hello" 파이프라인, andThen/compose 비교
- `ch14/collectors-groupingby.svg` — 요소들이 분류 함수를 거쳐 키별 버킷(Map)으로 모이는 과정

## 5. 검증

- 검수 25건: 항목별로 실제 파일 대조 후 적용 (전부 사실 — "검수 오류" 0건)
- ch1/ch2 소스: JDK 17 컴파일 통과 / ch13/ch14 소스: 전체 컴파일 + 신규·수정 메인 실행 확인
- 문서 이미지 참조 49개 전부 실제 파일 존재, 신규 SVG 2개 XML 유효
- 답안 링크 4개 전부 파일 존재 확인

## 6. 반영하지 않은 것

- 검수 ch13 "낮음"의 §7 부작용/불변성 비교 그림 — 함수 조합 그림 대비 효과가 낮다고 판단해 보류
- ch14 `스트림메인.java`의 ArrayList/Arrays/List 미사용 import — 검수 지적 범위 밖이라 유지 (필요시 다음 차수에서 일괄 정리 가능)
