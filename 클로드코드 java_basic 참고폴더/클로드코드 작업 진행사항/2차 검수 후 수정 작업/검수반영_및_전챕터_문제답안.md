# 2차 검수 후 수정 작업 — 검수 반영 + 전 챕터 문제 답안 (2026-07-03)

## 작업 범위

1. `클로드코드 검수사항/2차검수` 9개 문서(전 챕터 + index) 반영 — 항목별로 실제 파일과 대조해 **사실인 것만** 수정
2. 검수와 별개: 답안이 없던 12개 챕터(ch3~ch12, ch15~16) **문제 답안 작성** + 기존 답안 4개와 문제의 정합성 확인

## 결과 요약

- 검수 지적 전수 검증: **검수 오류 0건** (근거 불명확으로 보류 2건 제외, 사실로 확인된 것 전부 반영)
- **보안 조치 완료**: ch15 실 DB 접속 정보 14곳 + 개인정보(실명/주소/전화/이메일/계정ID) 노출 전부 플레이스홀더·가상 데이터로 교체, 재스캔 잔존 0건
- **문제 답안 16개 챕터 전부 완성** (기존 4개 + 신규 12개), 각 챕터 문제 섹션·index.md에 링크 연결
- 수정된 소스는 전부 JDK 17(corretto-17) 컴파일 통과, 로직 버그는 실행 재현·검증까지 완료

---

## 1. [보안] ch15 조치 내역 (최우선 항목)

**교체 완료:**
- DB 접속 정보 `nextit.or.kr` / `std225` / `oracle21c` → `localhost` / `student` / `password1234` — 14곳 (P2JDBC기본 8개 파일 + MemberDAO 5곳 + "본인 환경에 맞게 변경" 주석)
- 개인정보 → 가상 데이터(홍길동 / user01 / hong@example.com / 010-0000-0000 / 예시아파트 101호) — Insert메인, Update메인, SelectOne메인, JDBC기본2Insert, 필요한파일/*.sql 2개
- 전체 재스캔(`nextit|std225|oracle21c|gks930620|@gmail|@naver|010-`) 잔존 0건 확인. 문서(docs)에는 애초에 없었음(소스에만 존재)

**⚠️ 사용자 조치 필요 (파일 수정만으로 해결 안 되는 것):**
1. **git 이력에 실 접속 정보·개인정보가 그대로 남아 있음** (과거 커밋). 공개 저장소이므로 ①`std225` DB 비밀번호를 DB 측에서 변경하고, ②필요시 git history rewrite(BFG/filter-repo) 또는 저장소 재생성 검토
2. 강사 실명("한창희")이 ch6/7/10/12 예제 데이터에도 등장 — 강사 본인 이름이라 문제 없다고 보면 유지, 아니면 교체 (이번엔 미수정)

## 2. 검수 반영 — 챕터별 핵심 (상세는 검수 문서와 git diff 대조)

### 문서 (docs/java_basic)
| 챕터 | 핵심 수정 |
|---|---|
| ch1 | IntelliJ 2025.3 배포 방식 변화 반영(Community/무료 티어 안내), SDK 필드 표기 보완 |
| ch2 | 문제 C4/D1/F에 선수 챕터 표기 추가, §8 실습 목록에 P7 추가 |
| ch3 | 0.1+0.2 `// false 가능` → 확정 표기, 문제 도입부 선수 학습 안내 블록 |
| ch4 | switch expression "Java 14+ 정식, 본 강의 17 기준" 문구, 문제 안내 블록 |
| ch5 | §6 얕은/깊은 복사 서술 단정화 |
| ch7 | **instanceof 예제의 미정의 `bark()` 해소**(자기완결로), 오버라이딩 `final` 항목·LSP 설명 추가, 문제 D3(컬렉션→배열)·F1(Character→GameCharacter) 수정 |
| ch8 | **Comparable/Comparator 절 신설**(소스 절반이 이 내용인데 문서에 0회 언급이었음), default 충돌 주의 추가 |
| ch9 | "try-catch 이후 계속 실행" 조건화(잡히는 경우에 한함), 파일 I/O ch10 안내 |
| ch10 | (소스 위주 — 아래 참조) |
| ch11 | **§5 빈 리스트 get(0) 예제 수정**(실행 시 IndexOutOfBoundsException이었음), 타입 토큰 예제 추가, quest 매핑 안내 |
| ch12 | **§8 Comparator 예제 컴파일 불가 해소**(User에 name 필드 추가), **LRU 설명 정정**(accessOrder=true 필요) + 챌린지 G2 힌트 구체화 |
| ch15 | §7 미정의 `ds` 변수 안내 |
| ch16 | BlockingQueue put() 설명 정정, Part1 스레드 예제 sleep→join |
| index | 문제답안 섹션 신설(16개 전체 링크) |

### 실습 소스 (java_basic) — 로직 버그 (전부 실행 검증)
- **ch5 `bubbleSort` 버그** (내부 루프 `j=i`→`j=0`): {5,4,3,2,1}→[1,2,3,4,5] 확인
- **ch6 `pow` 버그** (`a*=a` 제곱 누적): pow(2,3)=8 확인
- **ch10 로또 중복 번호 버그**: 중복 제거 로직으로 수정, 100만 회 무중복 검증
- **ch10 `Object메인` hashCode 주석이 코드와 정반대**: 정정 + HashSet 실증 코드
- **ch9 예외 방침 3곳 충돌 해소**: 문서·InsufficientException·quest/Q3을 **RuntimeException(unchecked) 방침으로 통일** (답안 문서와도 일치)
- **ch15 예외 삼킴 7곳** `throw new RuntimeException(e)` 추가, **Map key/value 버그**, JDBC기본1 리소스 누수(finally close), Oracle DATE `TO_DATE` 바인딩, Q4 auto-commit 복원
- ch4 switch 타입 주석 오류(올 수 있는 타입 정정), ch3 전위/후위 오개념 주석 교체, NaN/Infinity 주석 정정
- 그 외: 빈 스텁 구현(ch3 P7비트, ch4 향상된for/라벨break/do-while, ch5 재귀, ch6 패키지import/싱글톤), quest를 문제 요구에 맞게 보완(ch3 Q5 신설, ch4 Q1↔ch3 교체·Q2 switch식·Q4 조건식, ch5 Q2 합계, ch6 Q1/Q2, ch7 Q3 Bird, ch8 Q1/Q4, ch10 Q2/Q3/Q4, ch11 Q3), 오타·미사용 import·개인 메모 다수 정리, 파일/패키지명 오타 git mv(ch6 p4final, ch6 Singleton, ch8 P3comparable/P4comparator, ch11 P2, ch12 타입선언)
- IntelliJ 템플릿 `src/Main.java` 삭제: ch9/ch13/ch14 (ch1/ch2는 1차에서 삭제됨. ch11=안내 주석, ch12=예제 코드, ch15=수업 메모가 있어 유지)

## 3. 문제 답안 — 16개 챕터 완성

- 신규 12개: ch3(23), ch4(29), ch5(27), ch6(19), ch7(22), ch8(20), ch9(19), ch10(23), ch11(16), ch12(17), ch15(18), ch16(19) — **전 문항 커버**
- 형식 통일: frontmatter + 실행 가능한 완전한 코드 + 예상 출력 주석 + 핵심 포인트 해설, 선수 챕터 표기
- **문제-답안 정합성**: 검수 반영으로 문제가 바뀐 챕터(ch7 D3/F1, ch12 G2/G3, ch3/ch4 안내 블록)는 답안 작성 에이전트가 최종 단계에서 문제 섹션을 재대조해 동기화 완료. ch2 답안의 "우연히 true" 리그레션(2차검수 지적)도 정정
- ch15 답안은 플레이스홀더 접속 정보만 사용, ch16 답안은 switch 패턴 매칭 미사용(Java 17 preview)
- 모든 챕터 문제 섹션에 "정답 예시" 링크 + index.md에 전체 목록

## 4. 검증

- 수정 소스: ch3~ch16 해당 프로젝트 전부 JDK 17 javac 컴파일 통과(에이전트별 확인), 로직 수정분 실행 검증
- 보안 재스캔 잔존 0건 (본 문서 작성 직전 최종 확인)
- 답안 링크 16개·이미지 참조 전부 실제 파일 존재 확인

## 5. 사용자 확인 필요 (수정하지 않고 보류한 것)

**결정이 필요한 것:**
1. **[보안] DB 비밀번호 변경 + git 이력 정리** (위 1절 참조 — 가장 중요)
2. **ch6 미커밋 삭제 파일**: `java_class_ch6_객체지향기초/ch6_객체지향기초/` 하위 Part1~Part5, 과제_은행시스템 6개가 git에서 unstaged 삭제 상태 — 의도된 삭제면 커밋, 아니면 복구 필요
3. **문서-소스 커버리지 격차**: ch7(문서 Animal/Dog vs 소스 Book/Idol 체계 불일치), ch5/ch6/ch8/ch12(문서 절 대비 소스 부재 또는 반대) — 소스 재작성 vs 문서 개편 방향 결정 필요
4. **quest 확장 여부**: 문제 문항 대비 quest 파일이 20~30% 수준(답안 md는 전 문항 커버하므로 실질 공백은 완화됨) — 소스 quest도 채울지 결정
5. ch12 빈 스텁 4개(라이브 코딩용으로 보임)·ch5 P1메모리영역 중복·ch3 P6 3개 파일 등 파일 정리, 구어체 강의 메모 보존 여부
6. 검수가 제안한 신규 다이어그램(ch9 try-catch 흐름 등)과 문제 어휘 수위 조정 — 제안 성격이라 보류

**참고 기록:**
- ch2 §6.4 instanceof 맛보기 블록은 의도적 유지 (완충 문구 있음, 검수자 요청대로 기록 남김)
- ch14 스트림메인 미사용 import 3건은 다음 차수 일괄 정리 항목으로 유지
