# 2026-07-02 작업 — java_basic 강의 전체 검수

## 무엇을 했나
검수자 역할로 java_basic 강의 전체(문서 16개 챕터 + index.md + 소스 프로젝트 16개, 자바 파일 284개, 이미지 폴더)를 검수하고, 발견한 문제점을 `클로드코드 검수사항` 폴더에 md 파일 9개로 정리했다. **강의 문서·소스 파일은 일절 수정하지 않았다(검수만 수행).**

## 검수 방법
- 챕터 2개씩 묶어 병렬 검수(문서-코드 대조, 이미지 링크 실존 확인, JDK 17 컴파일 검증 포함)
- 검수 관점: ① 기술적 오류(잘못된 설명/출력/컴파일 불가 코드) ② 문서-소스 불일치(예제·연습문제·이미지) ③ 초보자 관점 부족한 점(선수 개념 순서, 글로만 된 설명 → 이미지 보완 후보) ④ 오타 ⑤ 소스 품질(버그, 미사용 import, 빈 스텁, 개발 메모 노출)

## 산출물 (클로드코드 검수사항 폴더)
- `java_basic_검수_00_전체요약.md` — 즉시 수정 필요 15건 표 + 챕터 횡단 공통 문제 + 챕터별 파일 링크
- `java_basic_검수_ch1-ch2.md` ~ `java_basic_검수_ch15-ch16_index.md` (8개) — 챕터별 상세 발견 사항(심각도·파일:줄 표기)

## 주요 발견 (시급 순)
1. **ch15 소스에 실제 DB 접속 정보(호스트/계정/비밀번호)와 실제 개인 전화번호·이메일 하드코딩 노출** — 보안 최우선 조치 필요
2. ch15 `throw` 누락 4곳(예외가 조용히 삼켜짐), Map 매핑 key/value 버그
3. 로직 버그: ch6 `Calculator.pow`(2^3=256), ch5 `bubbleSort`(정렬 안 됨), ch14 `Human.java` 복붙 버그(toString/equals), ch10 로또 중복 버그
4. 문서 예제 오류: ch11 와일드카드 예제(실행 시 예외), ch12 Comparator 예제(컴파일 불가)
5. 구조적 문제(전 챕터 공통): 문서가 실습 소스를 참조하지 않아 연결 단절, 연습문제 대비 quest 풀이 커버리지 부족, 선수 개념 순서 위반, 강사 개인 메모 노출

## 다음 작업 제안
- 전체요약의 "즉시 수정 필요" 15건부터 작업자에게 전달해 수정
- ch15 DB 접속 정보는 git 이력에도 남아 있으므로 비밀번호 변경 등 별도 조치 검토
